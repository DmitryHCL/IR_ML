Example of usual workflow compiled in a single notebook.<br/>
Normally I would store all helper functions in a separate helper_functions.py.<br/>
Example includes most commonly used PLS method for predicting Research Octane Rating(RON) of gasoline.<br/>
Because sample size is usually pretty small (<100 samples) it is important to look for influential outliers.
